const help = require('./help');
const roles = require('./roles');
const events = require('./events');
const et = require('./et');
const eggs = require('./eggs');

module.exports = { help: help, roles: roles, events: events, eggs: eggs, et: et };
